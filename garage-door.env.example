# Example environment file for garage-door service
# Copy this to /etc/default/garage-door on the Pi and edit values.

# A strong secret token used by the web UI and API
GARAGE_TOKEN=replace-with-a-strong-random-token

# Port the app will listen on (optional)
PORT=8001

# GPIO pin (BCM)
RELAY_PIN=17

# Pulse duration in seconds
PULSE_SECONDS=0.5

# Rate limit for /trigger (Flask-Limiter syntax)
RATE_LIMIT='1 per 3 seconds'

# Log file path (relative to project directory)
LOG_FILE='logs/garage.log'

